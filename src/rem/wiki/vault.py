"""I/O layer for the wiki vault (files and git interaction)."""

class Vault:
    """Manages the reading, writing, and git versioning of the wiki pages."""
    def __init__(self) -> None:
        raise NotImplementedError("Vault is not implemented.")
